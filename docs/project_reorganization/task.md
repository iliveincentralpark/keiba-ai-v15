# 競馬予想Webアプリ 統合タスクリスト

## 実績（アーカイブ済）
- [x] UI/UX基盤の作成（グラスモーフィズム、ダークテーマ）
- [x] 模擬データによる買い目生成ロジックの実装
- [x] Netkeibaからのリアルタイムオッズ取得API (`scraper.py`) の実装と修正
- [x] フロントエンドへの実データ統合（`app.js` 内でのオッズマージ）
- [x] 予算分配ロジック、穴馬探索・オッズ歪み検知アルゴリズムの実装
- [x] `app.js` の安定性向上（エラーハンドリング、初期化、URLフォールバック）
- [x] Chrome動作確認 完了

## 現在のフェーズ：回収率シミュレーション
- [x] CSV拡張スクリプト（既存レコードの馬名・人気・オッズ自動取得）→ `enrich_csv.py`
- [x] シミュレーション画面 (`simulation.html` + `simulation.js`) 作成
- [x] `index.html` へのナビゲーション追加（📊成績分析ボタン）
- [x] ブラウザでのCSVアップロード動作確認

## 今後の拡張予定（バックログ）
- [ ] ユーザーの独自予想（特定の馬を強制的に軸にする等）の入力UI追加
